# Planned utilities

- `set-hostname.sh`: Safely change the hostname and FQDN after cloning a Debian or Ubuntu server.
- `docker-cleanup.sh`: Clean selected Docker data according to a procedure that will be provided later.
